# Public (publishable) Supabase configuration for the browser bundle.
# To move the app to another Supabase project: change these two values and
# re-run the migrations in supabase/migrations. Nothing else needs editing.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
